version: v1.0
name: Docker
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: Test
    task:
      jobs:
        - name: docker build
          commands:
            - checkout
            - docker build -t morad02/f1data .
        - name: Run test
          commands:
            - 'docker run  -tv `pwd`:/app/test morad02/f1data'
